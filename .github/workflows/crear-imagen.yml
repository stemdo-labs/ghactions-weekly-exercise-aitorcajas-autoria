name: Workflow para crear la imagen

on:
 workflow_dispatch:

jobs:
 Crear:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v3
  
  - name: Cargar nombre imagen
    id: cargar
    uses: ./action
 
  - name: Prueba
    run: echo "${{steps.cargar.outputs.image_name}}"
    
